# 凭证与本地配置（绝不提交）
.env
*.env.local

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
venv/
.pytest_cache/

# 探测/实验产出的结构化结果（含探测时的环境信息，不入库）
*.probe.json
out.json

# 训练/运行时产物
checkpoints/
outputs/
wandb/
*.log
# 但最终交付的训练日志必须入库：它是 reward 曲线（验收第 4 条）与
# §7.2 patch 质量统计的原始证据，评审需要能核对图表来源
!train_final_55steps.log

# 本地模型权重缓存（ModelScope 下载，已上传 COS，不入库）
.model_cache/

# TKE 节点池状态文件（运行时生成，下次 deploy 自动重建）
.gpu_nodepool_state.json

# 系统文件
.DS_Store
