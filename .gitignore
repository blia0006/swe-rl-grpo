# 凭证与本地配置（绝不提交）
.env
*.env.local

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
venv/
.pytest_cache/

# 探测/实验产出的结构化结果（含探测时的环境信息，不入库）
*.probe.json
out.json

# 训练/运行时产物
checkpoints/
outputs/
wandb/
*.log

# 本地模型权重缓存（ModelScope 下载，已上传 COS，不入库）
.model_cache/

# TKE 节点池状态文件（运行时生成，下次 deploy 自动重建）
.gpu_nodepool_state.json

# 系统文件
.DS_Store
